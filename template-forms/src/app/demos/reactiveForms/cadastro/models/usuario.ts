// Criando a model para tipar os dados do formulário
export class Usuario {
    id: string;
    nome: string;
    cpf: string;
    email: string;
    senha: string;
    confirmSenha: string;
}